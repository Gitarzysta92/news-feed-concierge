# Apply only for local development; Coolify routes the server through its proxy.
services:
  server:
    ports: ["127.0.0.1:8080:8080"]
  database:
    ports: ["127.0.0.1:5432:5432"]
  model:
    ports: ["127.0.0.1:11434:11434"]
